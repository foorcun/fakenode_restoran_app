import 'package:flutter/cupertino.dart';

class MainScreenGetx extends StatelessWidget {
  const MainScreenGetx({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("MainScreenGetx"),
    );
  }
}
